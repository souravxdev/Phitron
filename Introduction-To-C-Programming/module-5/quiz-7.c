#include<stdio.h>

int main(){

    int marks = 92;
    if (marks >= 80)
    {
        printf("A+");
    }
    if (marks >= 70)
    {
        printf("A");
    }
    if (marks >= 70)
    {
        printf("A-");
    }
    return 0;
}