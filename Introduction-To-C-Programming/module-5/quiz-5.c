#include <stdio.h>

int main()
{

    int marks = 76;
    if (marks >= 80)
    {
        printf("A+");
    }
    else if (marks >= 70)
    {
        printf("A");
    }
    else if (marks >= 70)
    {
        printf("A");
    }

    return 0;
}